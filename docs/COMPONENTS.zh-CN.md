# 组件状态（2026-08-27）

本页区分 **当前 APK 内置的 Winlator 专用包** 与 **官方上游最新 Release**。后者只有在存在可验证的 Android/ARM64/ARM64EC 构建产物并通过启动/渲染测试后才会进入默认列表。

| 组件 | 当前 3.1 内置/可选 | 上游最新 Release | 处理 |
|---|---|---|---|
| Box64 | 0.4.2 | v0.4.4（2026-08-02） | 保留 0.4.2；待生成 Winlator 专用 `.tzst` 后升级 |
| WOWBox64 | 0.4.2 | 跟随 Winlator 定制包 | 保留 0.4.2 |
| FEXCore | 2601 | FEX-2608（2026-08-05） | 保留 2601；2608 需重新构建/验证容器集成 |
| DXVK | 1.10.3 / 1.11.1-sarek / 2.3.1 及 ARM64EC 变体 | v3.0.2（2026-07-17） | 不直接塞官方桌面 tarball；等待兼容 Android/ARM64EC 包 |
| VKD3D-Proton | 2.8 / 2.14.1 | v3.0.1（2026-05-06） | 3.0.1 对移动/Turnip 有改进，但仍需 Winlator 包化和回归测试 |
| Turnip | adrenotools-turnip26.2.0 | Mesa/Turnip 持续滚动 | 3.1 已比 Plus 的 26.0.0 新，保持 26.2.0 |
| Gradle | 8.6 | 8.10.2（本分支采用） | 已升级到 8.10.2 |

## 为什么不“把版本号直接改成最新”

`app/src/main/assets` 中的 DXVK、VKD3D、Box64、FEX、Turnip 都是为 Winlator 打包的专用资源。只修改 `arrays.xml` 的版本号不会产生对应二进制，最终会造成组件选择后找不到文件或容器启动失败。

因此本分支采用：

1. 先保留 3.1 已验证且比 Plus 更新的资源；
2. 上游版本单独记录；
3. 只有拿到/构建对应 Winlator 包并验证后才加入默认组件列表；
4. CI 固定关键源码依赖提交，保证 Release 可复现。
